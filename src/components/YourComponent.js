// ...existing code...
// Remove the following lines
// import firebase from '../firebase';
// ...existing code...

// Remove any Firebase usage, for example:
// firebase.storage().ref().child('images/' + imageName).getDownloadURL().then((url) => {
//   setImageURL(url);
// });
// Replace with hardcoded image URLs
const imageURL = 'path/to/your/hardcoded/image.jpg';
// ...existing code...
